# Vite client envs
# For local dev, the Vite dev server proxies /api to http://localhost:5000 (see vite.config.js).
# If you prefer explicit absolute URLs, set VITE_API_BASE to your backend origin.
# Example for local dev:
# VITE_API_BASE=http://localhost:5000
# For same-origin proxy (recommended in dev), leave it empty:
VITE_API_BASE=